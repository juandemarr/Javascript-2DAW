/*Crea un script que determine si un número introducido por el usuario
es par*/
var numero=parseInt(prompt("Introduce un número"));
if (numero%2==0)
    document.write("El número "+numero+" es par");
else
    document.write("El número "+numero+" es impar");